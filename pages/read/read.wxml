<view wx:for="{{arr}}" wx:key="{{index}}">
  <view wx:if="{{index === num}}" class="box">{{item.title}}</view>
</view>
<view wx:for="{{list1}}" wx:key="{{index}}">
  <view class="box1">{{item.body}}</view>
</view>
<view class="box3">
  <view class="box2" bindtap="top">上一章</view>
  <view class="box4" bindtap="bottom">下一章</view>
</view>